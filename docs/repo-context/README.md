<!--
File: docs/repo-context/README.md
Purpose: Index of repos that have per-repo context docs.
Fill in: list each covered repo with a link to its folder.
See: docs/workspace-structure.md → "docs/ — Workspace Documentation"
-->

# Repo Context — Index

Per-repo navigation (`code-structure.md`) and architecture (`design.md`)
docs. None are populated yet.

Add one folder per covered repo: `docs/repo-context/<repo-name>/` containing
`code-structure.md` and `design.md`. Generate these once a codebase has
structure (a `prompt-library/` contract is the recommended way to keep the
output format consistent).

> None yet.
