---
description: Plan work too big for one session as a map of decision tickets (wayfinder), resolved one per session
argument-hint: "<loose idea to chart> | <map path> [ticket]"
---

Wayfinder: **$ARGUMENTS**

Execute the **wayfinder** workflow defined in `skills/wayfinder/SKILL.md` —
chart a new map from a loose idea, or work through an existing map one
decision ticket per session.

Tracker conventions for this workspace are in `docs/agents/issue-tracker.md`
→ "Wayfinding operations": the map is `work/<effort>/map.md`, tickets are
`work/<effort>/issues/NN-<slug>.md`. Resolved tickets are Tier-2 decisions
(see CONTEXT.md → Decision Records); promote lasting ones via `/decision promote`.
