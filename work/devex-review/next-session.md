# Catchup prompt — DevEx fix program: COMPLETE (no queued mission)

> **This file is the LAUNCHER (catch-up prompt).** Forward-only, REPLACED at
> each rollover: what to do next, still-binding constraints, pointers — never
> session history. Past-tense provenance lives in `handoff.md` (append-only
> ledger). Convention: docs/work-directory-conventions.md.

## Status

**The fix program is complete** (session 8, 2026-08-12): all 10 review-born
backlog cards (M19–M25, L32, L33, L35) are fixed and archived across fix
packages (a)–(e). The backlog stands at 1 open card — **M16**, a pre-review
finding owned by template-maintenance, not this program.

## Only possible follow-up

- If `git log --oneline -3` on main does not show the
  "Fix M23+L35 … (fix package (e))" commit, branch
  `worktree-devex-fix-package-e` is unmerged — ask the user to merge
  (fast-forward) and push; origin lagged local main at session-8 close.
- New DevEx findings: file backlog cards and work them under
  `work/template-maintenance/`, not here.

## Do NOT reload

- Any `findings/` file — every finding is either fixed or carded (M16).
- The persona review process — complete; never re-dispatch.
- Fix-package history — `handoff.md` top block, then archive.

This work item is dormant. Nothing is queued.
