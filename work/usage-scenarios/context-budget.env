# Per-work-item relaunch policy (docs/context-budget.md → "Relaunch knobs").
# usage-scenarios rolls over hands-free: background-launch the successor
# where the runtime supports it; falls back to manual elsewhere.
ROLLOVER_RELAUNCH=auto
