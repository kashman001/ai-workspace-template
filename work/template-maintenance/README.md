# template-maintenance

Ongoing work **on the ai-workspace-template itself**: backlog items
(`docs/template-workspace-backlog.html` — the issue tracker for this effort),
upstream skill syncs (`mattpocock/skills` reference clone), and template
feature additions.

Governing conventions: `CONTEXT.md` → "Template Backlog" (every fix/change/
discovery updates the backlog HTML), `docs/work-directory-conventions.md`.

**Start here:** `next-session.md` (launcher). History: `handoff.md` (ledger).
