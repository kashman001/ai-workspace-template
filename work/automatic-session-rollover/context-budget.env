# Per-work-item relaunch policy (committed; see docs/context-budget.md →
# "Relaunch knobs"). Overrides the workspace-global context-budget.env for
# this work item only; an explicit environment variable still wins.
ROLLOVER_RELAUNCH=auto
