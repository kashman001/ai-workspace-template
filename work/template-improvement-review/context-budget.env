# Per-item relaunch policy for work/template-improvement-review (committed).
# Overrides the root context-budget.env for this work item only; precedence in
# docs/context-budget.md -> "Relaunch knobs". The root default flipped to
# `manual` on 2026-09-16 (Stage 4 phase 0); this item keeps `auto` so its own
# supervised chains keep running once the new scripts are cut over.
ROLLOVER_RELAUNCH=auto
