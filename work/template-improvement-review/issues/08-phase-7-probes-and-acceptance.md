# 08 — Phase 7: probes rewritten; acceptance on Claude Code

**What to build:** the chain probe drops its prose criterion and passes on record fields only. The fleet probe drops the child-lock gate. Every probe that needs a vendor login gets a stub-runtime twin, and the twin is the CI contract. Two suite-level cases are added: supervisor restart with a spent stage, and the refused hand stage. Then the Claude Code acceptance runs on the throwaway work item: an attended rollover, then a two-session supervised chain. Those runs are the evidence the cutover needs.

**Blocked by:** 06 (Phase 5).

**Status:** ready-for-agent

- [ ] Chain probe passes on record fields only; no prose is asserted
- [ ] Fleet probe passes with the child-lock gate removed
- [ ] Every login-dependent probe has a stub twin; stub twins are green in CI
- [ ] Claude Code acceptance on the throwaway item: attended rollover passes the phase 4 assertions; a `--max-sessions 2` chain reaches `staged` once, then `quit_plain` or `cap`
- [ ] Acceptance evidence (record contents, verdicts) is recorded in the phase plan file
